<Window x:Class="MyOverlayPOC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:MyOverlayPOC"
        mc:Ignorable="d"
        Title=""
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Cursor="Arrow">

    <Window.Resources>
        <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border CornerRadius="4" Background="{DynamicResource Theme.ScrollThumb}" BorderBrush="Transparent" BorderThickness="0" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.DialogBackground}"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00E5FF" BlurRadius="20" ShadowDepth="0" Opacity="0.35"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="DialogLabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#00E5FF"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="#0A0E1A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#7B61FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#7B61FF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Theme.Input}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#00E5FF"/>
            <Setter Property="SelectionBrush" Value="#7B61FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00E5FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DialogCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextSecondary}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBoxBorder"
                                    Width="18" Height="18"
                                    BorderBrush="{DynamicResource Theme.Border}"
                                    Background="{DynamicResource Theme.Input}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                <TextBlock x:Name="checkMark" Text="✔"
                                           Foreground="#00E5FF"
                                           FontSize="13"
                                           FontWeight="Bold"
                                           Visibility="Collapsed"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="0,-3,0,0" />
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible" />
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="#00E5FF"/>
                            </Trigger>
                             <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="#00E5FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextMuted}"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0,8,0,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B60F1C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarPageButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkThinScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="8"/>
                    <Setter Property="Height" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0.00001*"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="{DynamicResource Theme.ScrollTrack}" CornerRadius="4"/>
                                    <Track Name="PART_Track" Grid.Row="0" IsDirectionReversed="true">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="ScrollBar.PageUpCommand" />
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="1,0,1,0" />
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButtonStyle}" Command="ScrollBar.PageDownCommand" />
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DarkThinScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" CanContentScroll="{TemplateBinding CanContentScroll}" Margin="{TemplateBinding Padding}" />
                            <ScrollBar Name="PART_VerticalScrollBar"
                                       Grid.Column="1" Grid.Row="0"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource DarkThinScrollBarStyle}"/>
                            <ScrollBar Name="PART_HorizontalScrollBar"
                                       Grid.Column="0" Grid.Row="1"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       Style="{StaticResource DarkThinScrollBarStyle}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Theme.SurfaceAlt}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Theme.SurfaceHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Theme.SurfacePressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
<Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.TextMuted}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="2">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FF00E5FF"/>
                    <Setter Property="Background" Value="#3300E5FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoadingSpinnerStyle" TargetType="Image">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="0" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                From="0"
                                To="360"
                                Duration="0:0:1.5"
                                RepeatBehavior="Forever" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="MainOverlayBorder" Padding="15" Background="{DynamicResource Theme.AppBackground}" CornerRadius="8" BorderBrush="{DynamicResource Theme.AppBorder}" BorderThickness="1">
        <TabControl x:Name="BrowserTabs" BorderThickness="0" SelectionChanged="BrowserTabs_SelectionChanged">
            <TabControl.Resources>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    
                    <Setter Property="AllowDrop" Value="True" />
                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TabItem_PreviewMouseLeftButtonDown" />
                    <EventSetter Event="PreviewMouseMove" Handler="TabItem_PreviewMouseMove" />
                    <EventSetter Event="Drop" Handler="TabItem_Drop" />
                    
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabItem}">
                                <Grid x:Name="templateRoot"
                                      Height="32"
                                      Margin="0,0,4,0"
                                      Width="105"
                                      ClipToBounds="True">

                                    <Border x:Name="DropSeparator"
                                            Width="2"
                                            Background="#00E5FF"
                                            HorizontalAlignment="Left"
                                            Margin="0,5,0,5"
                                            CornerRadius="1"
                                            Visibility="Collapsed"
                                            Opacity="1.0" />

                                    <Border x:Name="tabBorder"
                                            Background="{DynamicResource Theme.Surface}" 
                                            BorderBrush="{DynamicResource Theme.Border}"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                    </Border>

                                   
                                    <ContentPresenter x:Name="contentPresenter"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Center"
                                                      Margin="8,0,10,0"/>
                                </Grid>
                                <ControlTemplate.Triggers>

                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource Theme.TabSelectedBg}"/>
                                        <Setter TargetName="tabBorder" Property="BorderBrush" Value="{DynamicResource Theme.TabSelectedBorder}"/>
                                        <Setter TargetName="templateRoot" Property="Width" Value="135"/>
                                    </Trigger>
    
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource Theme.TabHover}"/>
                                    </Trigger>
    
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource Theme.TabSelectedBg}"/>
                                        <Setter TargetName="tabBorder" Property="BorderBrush" Value="#00E5FF"/>
                                    </MultiTrigger>

                                    <DataTrigger Binding="{Binding IsDragOver, RelativeSource={RelativeSource Self}}" Value="True">
                                        <Setter TargetName="tabBorder" Property="Background" Value="#00E5FF" />
                                        <Setter TargetName="tabBorder" Property="BorderBrush" Value="#80D8FF" />
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding IsTabDragging, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter TargetName="DropSeparator" Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabControl.Template>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

    
                        <DockPanel Grid.Row="0" Grid.Column="0">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="MuteStatusTextBlock"
                                           Text="Muted"
                                           VerticalAlignment="Center"
                                           Margin="0,0,5,0"
                                           Foreground="{DynamicResource Theme.TextPrimary}"/>
                                <StackPanel x:Name="RecordingStatusIndicator" 
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            Margin="0,0,10,0"
                                            Visibility="Collapsed">
                                    <Ellipse Width="10" Height="10" Fill="Red" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="RecordingTimerTextBlock" 
                                               Text="00:00"
                                               Foreground="{DynamicResource Theme.TextPrimary}"
                                               Margin="5,0,0,0"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <Button x:Name="MuteButtonInTemplate"
                                        Margin="0,5,5,5"
                                        Style="{StaticResource ModernButtonStyle}">
                                    <Image Source="./muted.png" Height="18" />
                                </Button>
                                <Button x:Name="MinimizeButton"
                                        Margin="0,5,5,5"
                                        Width="30">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Theme.SurfaceHover}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    <Image Source="./minimize.png" Height="23" />
                                </Button>

                                <Button x:Name="ExitButton"
                                        Margin="0,5,5,5"
                                        Width="30">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Theme.SurfaceHover}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    <Image Source="./x.png" Height="23" />
                                </Button>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,12,0">
                                    <Border Width="28" Height="28" CornerRadius="14" ClipToBounds="True" Margin="0,0,8,0" Background="#0A0E1A" BorderBrush="#00E5FF" BorderThickness="1.5">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#00E5FF" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                        </Border.Effect>
                                        <Image Source="./logo.png" Stretch="UniformToFill"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="OASYSS FLUX" Foreground="#00E5FF" FontWeight="Bold" FontSize="11" FontFamily="Segoe UI, Consolas"/>
                                        <TextBlock Text="DIVYESH EDITION" Foreground="#B388FF" FontSize="8" FontWeight="SemiBold" FontFamily="Segoe UI, Consolas" Margin="0,-1,0,0"/>
                                    </StackPanel>
                                </StackPanel>
 
                                <TabPanel x:Name="HeaderPanel" 
                                          IsItemsHost="true" 
                                          Margin="2,0,0,0" 
                                          ZIndex="1"
                                          AllowDrop="True"
                                          DragOver="HeaderPanel_DragOver"
                                          Drop="HeaderPanel_Drop" />
                                
                                <Button x:Name="NewTabButton"
                                        Margin="4,0,0,0" BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                            <ContentPresenter/>
                                                        </Border>
                                                    
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource Theme.SurfaceHover}"/>
                                                </Trigger>
    
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <Image Source="./plus.png" Width="15" Height="15" />
                                </Button>
                            </StackPanel>
                        </DockPanel>

                        <DockPanel Grid.Row="1" Grid.Column="0" Margin="5,0,5,10">
                            <Button x:Name="BackButton" Content="&lt;" Style="{StaticResource ModernButtonStyle}" Width="30" Margin="0,0,2,0" Height="30"/>
                            <Button x:Name="ForwardButton" Content="&gt;" Style="{StaticResource ModernButtonStyle}" Width="30" Margin="0,0,5,0" Height="30"/>
                            <Grid Width="30" Height="30" Margin="0,0,5,0">
                                <Button x:Name="RefreshButton" Style="{StaticResource ModernButtonStyle}" Height="Auto">
                                    <Image Source="./refresh.png" Height="23" VerticalAlignment="Center" />
                                </Button>
                                <Image x:Name="LoadingSpinner" Source="./loading.png"
                                       Style="{StaticResource LoadingSpinnerStyle}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Visibility="Collapsed"
                                       Height="23"/>
                            </Grid>
                            <Grid>
                                <TextBox x:Name="AddressBar"
                                         VerticalContentAlignment="Center"
                                         Padding="5,0"
                                         Background="{DynamicResource Theme.Surface}"
                                         Foreground="{DynamicResource Theme.TextPrimary}"
                                         BorderBrush="{DynamicResource Theme.Border}"
                                         BorderThickness="1"
                                         Height="30"/>
                                <TextBlock Text="Search Google or type a URL"
                                           Foreground="{DynamicResource Theme.TextMuted}"
                                           IsHitTestVisible="False"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=AddressBar}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DockPanel>

                        <Border Grid.Row="2" Grid.Column="0" Background="{DynamicResource Theme.Surface}" CornerRadius="4" Margin="5,0,5,10" Padding="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <ItemsControl x:Name="BookmarksBar" ItemsSource="{Binding Bookmarks}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type local:Bookmark}">
                                                <Button Click="Bookmark_Click" 
                                                        MouseRightButtonUp="Bookmark_RightClick" 
                                                        Style="{StaticResource ModernButtonStyle}" 
                                                        Margin="2,0" 
                                                        Padding="5,4">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Image Source="{Binding Favicon}" Width="16" Height="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" MaxWidth="90" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                <Button Grid.Column="1" 
                                        x:Name="AddBookmarkButton" 
                                        Width="30" 
                                        Height="30" 
                                        Margin="0,0,0,0" 
                                        Click="AddBookmarkButton_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource Theme.SurfaceHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <Image Source="./plus.png" Height="23"/>
                                </Button>

                                <!-- AI Chat Toggle Button -->
                                <Button Grid.Column="2" x:Name="AiChatButton" Style="{StaticResource ModernButtonStyle}" Width="30" Height="30" Margin="5,0,0,0" ToolTip="Open AI Chat">
                                    <TextBlock Text="🤖" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Button>

                                <!-- Transparency Toggle Button + Slider Popup -->
                                <StackPanel Grid.Column="3" Orientation="Vertical" Margin="5,0,0,0">
                                    <Button x:Name="TransparencyButton" Style="{StaticResource ModernButtonStyle}" Width="30" Height="30" ToolTip="Toggle Transparency (Shift+Alt+T)">
                                        <TextBlock Text="👁" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Button>
                                    <Border x:Name="TransparencySliderPopup" 
                                            Visibility="Collapsed" 
                                            Background="{DynamicResource Theme.Surface}" 
                                            BorderBrush="{DynamicResource Theme.Border}" 
                                            BorderThickness="1" 
                                            CornerRadius="4" 
                                            Padding="5" 
                                            Margin="0,2,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Opacity" Foreground="{DynamicResource Theme.TextMuted}" FontSize="10" HorizontalAlignment="Center"/>
                                            <Slider x:Name="TransparencySliderControl" 
                                                    Minimum="0.1" Maximum="1.0" Value="0.5" 
                                                    Width="80" 
                                                    Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <Button Grid.Column="4" x:Name="SettingsButton" Style="{StaticResource ModernButtonStyle}" Width="30" Height="30" Margin="5,0,0,0" Click="SettingsButton_Click">
                                    <Image Source="./settings.png" Height="23" VerticalAlignment="Center">
                                        <Image.Effect>
                                            <DropShadowEffect Color="Black" ShadowDepth="0" BlurRadius="4" Opacity="1"/>
                                        </Image.Effect>
                                    </Image>
                                </Button>
                            </Grid>
                        </Border>

                        <Border Grid.Row="3" Grid.Column="0"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Background="Transparent"
                                Padding="0"
                                CornerRadius="0,0,14,14">
                            <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent"/>
                        </Border>

                        <Border x:Name="WebViewPlaceholder"
                                Grid.Row="3" Grid.Column="0"
                                Background="#202124"
                                Opacity="0.5"
                                CornerRadius="0,0,14,14"
                                Visibility="Collapsed" />

                        <!-- AI Chat Panel Host (populated dynamically in code-behind) -->
                        <Grid x:Name="AiChatHost"
                              Grid.Row="3" Grid.Column="0"
                              Visibility="Visible" />

             
                        <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="4"
                              x:Name="ScreenshotTray" Visibility="Collapsed"
                              Margin="5,5,0,5" MaxHeight="{Binding ElementName=MainOverlayBorder, Path=ActualHeight}">
                            <Border Background="{DynamicResource Theme.Surface}" CornerRadius="4" Padding="5">
                                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Style="{StaticResource DarkThinScrollViewerStyle}">
                                    <ItemsControl x:Name="ScreenshotsContainer" ItemsSource="{Binding Screenshots}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Vertical"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type local:Screenshot}">
                                                <Grid Margin="5">
                                                    <Border CornerRadius="6" BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1" Background="#111">
                                                        <Image Source="{Binding Image}"
                                                               Height="84"
                                                               Stretch="Uniform"
                                                               MouseMove="Screenshot_MouseMove"/>
                                                    </Border>
                                                    <Button Content="x"
                                                            HorizontalAlignment="Right"
                                                            VerticalAlignment="Top"
                                                            Margin="0,-4,-4,0"
                                                            Width="18" Height="18"
                                                            FontSize="10"
                                                            FontWeight="Bold"
                                                            Foreground="White"
                                                            Background="#D0FF0000"
                                                            BorderBrush="White"
                                                            BorderThickness="1"
                                                            Click="DeleteScreenshot_Click">
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Grid>
                                                                    <Ellipse Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}"/>
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
        </TabControl>
    </Border>
</Window>